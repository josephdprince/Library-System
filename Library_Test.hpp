#ifndef __LIBRARY_TEST_HPP__
#define __LIBRARY_TEST_HPP__

#include <iostream>

#include "gtest/gtest.h"
#include "Library.hpp"

using namespace std;

TEST(LibraryTest, temp) {
	Library* lib = new Library();
	//Book b = Book("stri", "strin", "string", 0, 0, 0);
}

#endif
